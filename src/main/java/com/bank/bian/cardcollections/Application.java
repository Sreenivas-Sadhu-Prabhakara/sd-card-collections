package com.bank.bian.cardcollections;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.annotation.Bean;

/**
 * BIAN Service Domain: Card Collections
 *
 * Business Area:      Operations and Execution
 * Business Domain:    Cards
 * Functional Pattern: Process
 * Asset Type:         Card Collection Case
 * Control Record:     Card Collection Case Procedure
 *
 * Generated by the bian-platform service generator (Phase 1 — shallow build).
 * Replace the in-memory store with real persistence and domain logic in Phase 2.
 */
@SpringBootApplication
public class Application {

    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }

    @Bean
    public OpenAPI openApi() {
        return new OpenAPI().info(new Info()
                .title("Card Collections — BIAN Semantic API")
                .description("Service Domain 'Card Collections' (Process pattern) "
                        + "operating on the 'Card Collection Case Procedure' control record.")
                .version("0.1.0"));
    }
}
